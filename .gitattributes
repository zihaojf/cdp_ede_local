# These files carry the lab answers on the solution branch
# (filled-in minicpu, fixed exp4 design). "merge=ours" makes each
# branch keep its own version of these files whenever both sides
# have modified them since the merge base, so merging will never
# silently swap the student version for the answers (or vice versa).
# Requires: git config merge.ours.driver true
minicpu_env/miniCPU/minicpu_top.v merge=ours
dc_env/exp4/show_sw.v merge=ours
